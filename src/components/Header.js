import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import {
	faBars,
} from '@fortawesome/free-solid-svg-icons';

const Header = () => {
	return <header><FontAwesomeIcon icon={faBars} /></header>
}

export default Header;